
@{
    ViewData["Title"] = "Fravær";
}

<h3>Fravær</h3>
@* TODO: finn alternativ til URL.Action (taghelper) *@
<div class="alert alert-dismissible alert-info">
    <button type="button" class="close" data-dismiss="alert">&times;</button>
    <h4>Til informasjon!</h4>
    <p><strong>Fraværsgrunn</strong> Må velges i listen under.<a href="#" class="alert-link"> Velg fraværsgrunn og fyll ut skjema på neste side</a>.</p>
</div>
<div class="Btn-layout">
    <div class="text-center">
              
    </div>
    <div class="form-group">
        
        <label for="select" class="col-lg-2 control-label">Fraværsgrunnlag:</label>
        <div class="col-lg-10">
            <select class="form-control" id="select1" onchange="location = this.value;">
                <option value="@Url.Action("Index", "Absence")">Velg fraværsgrunn</option>
                <option value="@Url.Action("Sick", "Absence")">Egenmelding (sykdom)</option>
                <option value="@Url.Action("SickChild", "Absence")">Egenmelding (sykt barn)</option>
                <option value="@Url.Action("SickLeave", "Absence")">Sykemelding</option>
            </select>
        </div>
    </div>
</div>
